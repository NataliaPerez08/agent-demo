apiVersion: v1
kind: Service
metadata:
  name: chatbot
  namespace: ${namespace}
spec:
  selector:
    app: chatbot
  ports:
    - port: 8001
      targetPort: 8001
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: chatbot
  namespace: ${namespace}
spec:
  replicas: 1
  selector:
    matchLabels:
      app: chatbot
  template:
    metadata:
      labels:
        app: chatbot
    spec:
      containers:
        - name: chatbot
          image: ${chatbot_image}
          imagePullPolicy: Always
          ports:
            - containerPort: 8001
          env:
            - name: AGENT_API_URL
              value: http://api:8000
          readinessProbe:
            httpGet:
              path: /health
              port: 8001
            initialDelaySeconds: 5
            periodSeconds: 5